services:
  api:
    build: .
    ports:
      # IPv4 loopback only: Docker Desktop on Windows often breaks [::1]:published-port;
      # Chrome uses IPv6 for localhost first → ERR_EMPTY_RESPONSE or long hang.
      - "127.0.0.1:8080:80"
